#include <bits/stdc++.h>

using namespace std;

int main() {
  string name = "Antar";
  // Concat
  // name += " Das";
  // Compare (Equal)
  /**
   * Equal
   *
   */
  // Resize string
  // name.resize(2);

  // Clear string
  //  name.clear();

  // Check if string is empty
  // if(name.empty()){
  //   cout<<"String is empty"<<endl;
  // }

  // Access nth element of string
  // cout<<name[0]<<endl;
  // cout<<name.at(2)<<endl;

  // String Concatenation
  // name.append(" Das");
  // name.push_back('G');

  // cout << name << endl;
  string name[12];
  cin>>name;
  cout<<name;
}