#include <bits/stdc++.h>

using namespace std;

int main() {
    // int array[100];
    // memset(array, -1, sizeof array); // initializing array to -1
    // int n;
    // cin >> n;
    // for(int i = 0; i < n; i++){
    //     cout << array[i] << " ";
    // }
    int i = 6;
    cout << i + (i & (-i));
    return 0;
}